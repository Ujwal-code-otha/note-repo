name: Selenium Web E2E Suite

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  selenium-tests:
    name: Run Selenium Web Tests
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_FIREBASE_API_KEY: 'mock_api_key'
      NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: 'mock.firebaseapp.com'
      NEXT_PUBLIC_FIREBASE_PROJECT_ID: 'mock-project'
      NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: 'mock.appspot.com'
      NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: '123456789'
      NEXT_PUBLIC_FIREBASE_APP_ID: '1:123456789:web:mock'
      NEXT_PUBLIC_GEMINI_API_KEY: 'mock_gemini_key'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'
          cache-dependency-path: website/package.json

      - name: Install Website Dependencies
        working-directory: website
        run: npm ci

      - name: Start Web Server
        working-directory: website
        run: |
          npm run dev -- -H 127.0.0.1 > server.log 2>&1 &
          SERVER_PID=$!
          sleep 30
          if ! kill -0 $SERVER_PID 2>/dev/null; then
            echo "Server process died. Logs:"
            cat server.log
            exit 1
          fi

      - name: Wait for Web Server (Port 3000)
        run: npx wait-on tcp:127.0.0.1:3000 --timeout 120000 --verbose

      - name: Verify Server Health
        run: |
          curl -fL http://127.0.0.1:3000 || (echo "Server health check failed. Server logs:" && cat website/server.log && exit 1)

      - name: Additional Wait Buffer
        run: sleep 5

      - name: Run Selenium NodeJS Tests
        working-directory: website
        run: node tests/seleniumTest.js
        env:
          HEADLESS: 'true'
          TEST_EMAIL: 'shreyassatishkumar@gmail.com'
          TEST_PASSWORD: '123456'

      - name: Grant Execute Permission to Gradle Wrapper
        run: chmod +x gradlew

      - name: Run Selenium Kotlin Gradle Tests
        run: ./gradlew :selenium-automation:test
        env:
          TEST_EMAIL: 'shreyassatishkumar@gmail.com'
          TEST_PASSWORD: '123456'

      - name: Upload Server Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Web-Server-Log
          path: website/server.log
          retention-days: 7

      - name: Upload Selenium JS Excel Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Selenium-JS-Excel-Report
          path: website/reports/selenium-report_*.xlsx
          retention-days: 7

      - name: Upload Selenium Gradle Test Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Selenium-Gradle-Test-Reports
          path: selenium-automation/build/reports/tests/test/
          retention-days: 7


